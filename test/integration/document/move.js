describe('Integration', () => {
  describe('document', () => {
    describe('#move', () => {
      after(() => { clearContentWithEntities(); })

      xit('entity', () => {

      })

      xit('undo move entity', () => {

      })

      xit('redo move entity', () => {

      })
    })
  })
})
